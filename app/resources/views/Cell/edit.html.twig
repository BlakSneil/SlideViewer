{% extends 'Cell/base.html.twig' %}

{% block content %}

    <h4>Cell: {{ name }}</h4>

    <br>

    {{ form_start(form) }}
    {{ form_errors(form) }}

    {{ form_rest(form) }}

    <br>

    <input type="submit" class="btn btn-success" value="Save" />

    {{ form_end(form) }}

{% endblock content %}
